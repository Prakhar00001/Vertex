<nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
  <div class="flex items-center space-x-4">
    <a href="/" class="font-bold text-xl tracking-tight text-indigo-600">VERTEX</a>
  </div>
  <div class="flex items-center space-x-4 text-sm">
    <% if user_signed_in? %>
      <span class="text-gray-600"><%= current_user.name %></span>
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-red-600 hover:underline bg-transparent border-0 cursor-pointer" %>
    <% else %>
      <%= link_to "Sign In", new_user_session_path, class: "text-indigo-600 font-medium" %>
    <% end %>
  </div>
</nav>